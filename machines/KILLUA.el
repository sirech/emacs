;;; KILLUA.el --- Settings specific to a machine

;; (set-frame-size-according-to-resolution)
(setq initial-frame-alist
      '((left . 0) (top . 0)
	(width . 155) (height . 53)))

;; Add cygwin to path
(add-to-list 'exec-path "C:/cygwin/bin")
